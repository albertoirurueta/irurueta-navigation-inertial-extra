= Installation
:description: How to add irurueta-navigation-inertial-extra to a Java project via Maven, including release and snapshot dependency coordinates, requirements, and building from source.
:keywords: Maven dependency, Java 21, Java library installation, Maven Central, snapshot repository, build from source, irurueta-navigation-inertial-extra

NOTE: This documentation was generated with the assistance of AI. Please report any inaccuracies.

WARNING: This library is **experimental** — see xref:index.adoc#_background_and_references[Background and
references] for details. APIs may still change between minor releases.

`irurueta-navigation-inertial-extra` is published to Maven Central as a standard Java library and can be added as
a Maven dependency.

== Latest release

[source,xml]
----
<dependency>
    <groupId>com.irurueta</groupId>
    <artifactId>irurueta-navigation-inertial-extra</artifactId>
    <version>1.8.0</version>
    <scope>compile</scope>
</dependency>
----

== Latest snapshot

[source,xml]
----
<dependency>
    <groupId>com.irurueta</groupId>
    <artifactId>irurueta-navigation-inertial-extra</artifactId>
    <version>1.9.0-SNAPSHOT</version>
    <scope>compile</scope>
</dependency>
----

Snapshot artifacts are published from the `develop` branch; add the Sonatype snapshots repository if you need to
resolve snapshot versions:

[source,xml]
----
<repository>
    <id>sonatype-snapshots</id>
    <url>https://central.sonatype.com/repository/maven-snapshots/</url>
    <releases>
        <enabled>false</enabled>
    </releases>
    <snapshots>
        <enabled>true</enabled>
    </snapshots>
</repository>
----

== Requirements

* Java 17 or later (the project compiles with `maven.compiler.source`/`target` set to 17).
* Maven (the project is built and tested with the standard Maven lifecycle: `mvn clean install`).

== Building from source

[source,bash]
----
git clone https://github.com/albertoirurueta/irurueta-navigation-inertial-extra.git
cd irurueta-navigation-inertial-extra
mvn clean install
----
